/**
 * Default export for @overlay parallel route.
 * Returns null when no overlay route is active.
 * Required by Next.js for parallel routes.
 */
export default function OverlayDefault() {
	return null;
}
